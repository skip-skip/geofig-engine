# Phase 14.59: Commit majortick implementation

**Status**: open
**Phase**: 14.59
**Dependencies**: `14_59_wp16_verify`

## Description

Commit the completed majortick work in the established Phase 14.59 style,
after review of `git status`/`git diff`. Then move this and all dependency
task files from `tasks/open` to `tasks/closed` with **Status**: complete.

Suggested message:

    Phase 14.59: add configurable majorticks (length/offset/width/color) across all four frame edges

## Files to modify

- none (git + task-file housekeeping)

## Acceptance criteria

- [ ] All intended files staged (no secrets/stray files)
- [ ] Commit message matches repo style
- [ ] Task files `14_59_wp8`..`14_59_wp17` moved to `tasks/closed` with
      status complete
- [ ] Working tree clean